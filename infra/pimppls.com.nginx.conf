server {
  listen      [::]:443 http2;
  listen      443 http2;
  root /var/www/@/html;
  server_name pimppls.com;
  access_log  /var/log/nginx/admin-access.log;
  error_log   /var/log/nginx/admin-error.log;

  error_page 404 /404.html;

  location / {
    add_header  X-Robots-Tag "noindex, nofollow, nosnippet, noarchive";
    gzip on;
    gzip_min_length  1100;
    gzip_buffers  4 32k;
    gzip_types    text/css text/javascript text/xml text/plain text/x-component application/javascript application/x-javascript application/json application/xml  application/rss+xml font/truetype application/x-font-ttf font/opentype application/vnd.ms-fontobject image/svg+xml;
    gzip_vary on;
    gzip_comp_level  6;
  }

  location ~* \.(jpg|jpeg|png|gif|ico|js|css|svg)$ {
    expires 7d;
  }

  ssl on;
  ssl_certificate /etc/letsencrypt/live/api.pimppls.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.pimppls.com/privkey.pem;
}

server {
  listen      [::]:443 http2;
  listen      443 http2;
  server_name www.pimppls.com;
  rewrite ^ https://pimppls.com$request_uri? permanent;
}

server {
  listen      [::]:80;
  listen      80;
  server_name pimppls.com www.pimppls.com;
  rewrite ^ https://pimppls.com$request_uri? permanent;
}
