#!/bin/sh

cd ..; git subtree push --prefix scraper dokku-scraper master; cd -

# forced deploy (from root directory):
# git push dokku-scraper `git subtree split --prefix scraper master`:master --force
