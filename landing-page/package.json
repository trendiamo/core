{
  "name": "landing-page",
  "description": "The Trendiamo landing page",
  "version": "0.0.1",
  "author": "Trendiamo Team",
  "ava": {
    "verbose": "true"
  },
  "bugs": "",
  "dependencies": {
    "lazysizes": "^4.1.4",
    "reshape-standard": "^3.3.0",
    "spike": "^2.3.0",
    "spike-css-standards": "^4.0.0",
    "spike-js-standards": "^2.1.0",
    "spike-optimize": "^0.1.2",
    "spike-page-id": "^1.1.0",
    "tingle.js": "^0.13.2",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "ava": "^0.19.1",
    "rimraf": "^2.6.1",
    "snazzy": "^7.0.0",
    "standard": "^10.0.2"
  },
  "homepage": "https://trendiamo.com/",
  "main": "app.js",
  "private": true,
  "repository": "",
  "scripts": {
    "lint": "standard | snazzy",
    "precommit": "npm run lint -s",
    "pretest": "npm run lint -s",
    "test": "NODE_ENV=test ava",
    "start": "spike watch",
    "build": "spike compile",
    "prod": "NODE_ENV=production spike compile -e production",
    "deploy": "rm -rf public/* && yarn lint && yarn prod && rsync -azh -e ssh --delete-after --ignore-errors public/ root@139.59.128.112:/var/www/html"
  }
}
